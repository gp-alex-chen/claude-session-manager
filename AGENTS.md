# Codex / 主代理 / Sub Luna / Sol 协作规范

本仓库采用“主代理执行、Sub Luna 分包、Sol 提供咨询”的协作方式。主代理不绑定具体模型，可以使用当前配置的 Luna 或其他可用模型；Sub Luna 是按需分包的 Luna 执行者；Sol 是按需调用的 subagent，使用模型 `gpt-5.6-sol`，只负责提供咨询建议。

## 角色与职责

- 主代理负责理解任务、与用户沟通、诊断、实现、测试、提交和最终整合；可以自行编写核心代码，也可以把边界清晰、可独立验证的子任务分包给 Sub Luna。
- 分包时必须明确目标、允许修改的文件、验证方式、交付物和思考等级。思考等级指模型的 reasoning effort：低用于机械、局部、低风险任务；中用于单模块和有限联动；高或更高用于复杂设计、跨模块修改或高风险回归；只使用当前模型支持的档位。
- Sub Luna 使用 `gpt-5.6-luna`，只负责主代理分配的范围，不得改变方向、扩大文件范围、切换/创建/删除分支或代替主代理做最终整合。模型不可用时必须明确说明，不得静默冒充其他模型。
- Sol 是使用 `gpt-5.6-sol` 的咨询 subagent，只负责复杂 plan、合理尝试后仍阻塞的问题和用户要求的只读审核；不直接编辑、提交或替用户做最终决定。调用时由主代理提供目标、基线、证据、约束和具体问题，并要求给出建议、必须修正项和明确结论。模型不可用时必须明确说明，不得静默替换。
- Sol 的回复是建议，不会自动扩大既有授权。只有建议涉及既有目标、范围、方向或集成方式之外的变更时，主代理才须先向用户转达并取得二次确认；在既有授权范围内，主代理可继续正常实现、测试和提交。
- 只有主代理可以创建代理或会话。Sub Luna 和 Sol 不得调用 `spawn_agent`、`create_thread`、`fork_thread`、`send_message_to_thread` 或其他派生能力，不得再创建 subagent 或二次会话。
- 一个会话默认只使用一个 Sol；后续复审必须复用已有 Sol 线程，不新建第二个。不同专业领域确需多个 Sol 时，由主代理说明边界并统一整理结论。
- 主代理不得主动关闭 Sub Luna 或 Sol 会话；仅在用户明确要求时关闭。
- 主代理必须独立 inspect 所有改动、检查残留引用并按风险复跑测试；最终回报包含设计或根因、修改文件、测试结果、commit hash、`git status` 和遗留风险。

## 任务节奏

1. 普通任务由主代理确认基线和允许修改范围后执行；复杂 plan 或合理尝试后仍阻塞时，按 Sol 咨询流程处理。
2. 强制一步一任务：每一步只有一个语义边界，完成并经主代理审查通过后才能开始下一步；改变已确认方向时重新沟通。
3. 诊断与修复分离。用户只要求诊断时不得编辑或提交；修复类任务先补一个修复前会失败的确定性测试，再做最小修复。
4. 分包任务必须有独立文件所有权。并行 Sub Luna 必须使用各自的独立 worktree，禁止共享同一工作目录；相同或重叠文件即使 worktree 不同也必须串行处理。主代理可只读分析，但不得修改 Sub Luna 的文件。
5. 严格禁止无关改动：不顺手重排文件、升级依赖、制造整批 CRLF 噪声、删除用户残留或重写已有提交；测试不依赖睡眠、真实用户数据或偶然的本机环境。
6. 主代理调用会产生可见独立会话的工具前，必须向用户说明。`fork_context: true` 不作为 Sol 咨询的默认方式；只传递完成任务所需的最小上下文。

## Git 与数据安全

- 开始工作先确认分支、HEAD、工作树和 stash。保留用户已有修改、未跟踪个人文件及 stash；发现重叠时停止并报告，不覆盖。
- 创建或切换分支、worktree 前，必须先同步 `origin/main`，并确认当前工作树 `git status --short` 为空、没有未跟踪文件；检查 stash，若存在与任务文件重叠或无法确认归属的 stash 冲突，立即停止并报告，不自动应用或丢弃。并行的独立任务默认从同步后的最新 `origin/main` 创建各自的 `codex/*` 分支和 worktree；禁止从其他 agent 的功能分支或工作树分支继续。
- 后续分支会继承基线的全部祖先提交；只有确实依赖前序任务的 follow-up 才能从前序分支建立，并必须记录依赖分支、基线 commit 和会继承的提交。独立任务发现基线不是 `main` 时必须停止并报告。
- Sub Luna 必须留在主代理分配的当前分支和 worktree，不得自行 switch、创建或删除分支；可以提交分配范围内的代码，但不得 push、merge 或代替主代理整合。
- 不得擅自 `push`、`merge`、删除分支、移动/覆盖 tag 或发布 release。只有用户明确授权后，主代理才能集成、推送或发布；集成前必须审查完整祖先链和 diff，不得重写或覆盖已有提交。
- fast-forward 只用于目标分支可直接前移且无分叉时，优先用于本地 `main` 同步或整合单条提交链；rebase 只允许用于未推送、未共享的本地提交，以便基于最新 `origin/main` 整理，禁止改写已推送提交和 force-push。
- squash 只在集成独立任务时使用，用于压缩目标分支上的任务提交；已推送分支不通过 squash/rebase 改写其历史。已推送分支默认用 merge 保留提交链；只需单个语义提交时可 cherry-pick，并记录来源 commit。
- 发生冲突时，由主代理按目标选择 merge 或 cherry-pick 解决，检查完整 diff 后复跑相关测试；不得盲选 `ours`/`theirs`。冲突涉及用户未提交、未跟踪或无法确认归属的修改时，立即停止并报告。
- 用户只要求实现、编译或测试时，默认不得 commit；自动测试通过不等于用户验收通过。只有用户明确说“提交/commit”，或任务授权明确包含提交时，主代理才可 commit。
- 提交按一个语义任务切分，消息清楚；不使用危险的 reset、checkout 覆盖、宽范围删除或清理命令，不删除用户本地 exe、状态 JSON、日志或构建残留。
- 构建产物、临时 exe 和 Go 缓存放入被忽略的 `.codex-build/` 或项目内缓存目录；不得把 `frontend/dist`、`node_modules`、用户状态文件纳入提交。

## 版本与发布

- RC bug 使用新的递增 RC tag 修复，不能移动或覆盖已经发布的 tag。
- 应用 updater 只识别稳定 `v*-wails` 版本；预发布 tag 的识别和发布条件必须与 README、CI 和 updater 规则一致。
- 发布前主代理必须确认构建版本注入、产物名称、Windows PE 头（`MZ`）、大小及必要时的 hash。发布后继续监控 CI、artifact 和 release 状态，不把“上传成功”当作完整验收。

## 测试矩阵

按改动范围和风险选择测试，主代理负责执行与独立复核；Sub Luna 可执行分配范围内的验证：

- 前端：`npm ci`（需要时）、`npm test`、`npm run build`。
- Go：`go test ./...`、必要时 `go test -race ./...`、`go vet ./...`；提交前对修改过的 Go 文件运行 `gofmt`。
- 终端或 agent 边界：必要时运行明确标记的 Windows ConPTY / Claude integration 测试；普通单测不得依赖真实 ConPTY、Claude CLI 或用户目录。
- 生产构建：使用项目规定的 Windows tags、`GOOS=windows GOARCH=amd64 CGO_ENABLED=0` 和版本 ldflags，检查 exe 存在、非零大小、前两字节为 `MZ`，必要时记录 hash。
- 恢复、持久化和生命周期 bug 必须覆盖完整语义：`shutdown → persist → startup → restore`。不能只测前端 UI 单点，也不能只断言某个 Wails 方法被调用。
- 测试失败时区分代码失败、环境限制和网络/权限限制，透明报告命令、退出结果及未做的端到端验证。

## Windows 与仓库约定

- 项目是 Windows-only；状态文件（favorites、open-sessions、settings）默认位于 exe 同目录。测试通过临时目录或依赖注入隔离用户数据。
- 不删除用户本地 exe、state JSON、日志或旧残留；构建文件使用 `.codex-build`。不要把个人路径、token、密钥或本机数据写入源码、测试和文档。
- 避免造成大规模 CRLF 噪声。只格式化实际修改的文件；如果工具产生整文件换行 diff，应停止并改用保留现有格式的方式。
- README、`docs/maintenance.md`、CI、Wails wrapper 和实际目录结构必须保持一致；新增公开绑定时同时检查 Go 方法、wrapper、frontend API 边界和绑定测试。

## 沟通与交接

- 主代理在长任务中持续向用户发送简短进度；对未运行的 GUI、真实 Claude、真实 ConPTY 或发布后验证必须明确说明。
- Sol 咨询完成后，主代理必须先向用户转达 Sol 的结论和建议，不得把 Sol 的回复当作用户授权。
- 主代理和 Sub Luna 的最终回报至少包含：根因或设计、修改文件、测试命令及结果、提交 hash、`git status` 和遗留风险；只做诊断时明确“未编辑、未提交”。
- 主代理审查不通过时，只派发针对该步骤的 follow-up 修复；修复后重新测试，再决定是否进入下一步。
